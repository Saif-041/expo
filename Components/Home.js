import { StyleSheet, Text } from 'react-native';

export default function Home() {
  return (
  <Text>
    New Home Screen
  </Text>
  );
}

const styles = StyleSheet.create({
  image: {
    width: 320,
    height: 440,
    borderRadius: 18,
  },
});